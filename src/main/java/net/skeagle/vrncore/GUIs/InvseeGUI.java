package net.skeagle.vrncore.GUIs;

public class InvseeGUI {

    /*public InvseeGUI(List<NBTTagCompound> nbt, OfflinePlayer p) {
        super("Viewing " + p.getName() + "&r's " + "inventory", nbt);
    }

    @Override
    protected ItemStack convertToItem(NBTTagCompound nbt) {
        if (!nbt.isEmpty())
            return CraftItemStack.asBukkitCopy(net.minecraft.server.v1_16_R3.ItemStack.a(nbt));
        return new ItemStack(Material.AIR);
    }

    @Override
    protected void onClick(NBTTagCompound nbtitem, InventoryClickEvent e) {
        if (p.getInventory().firstEmpty() == -1)
    }
     */
}